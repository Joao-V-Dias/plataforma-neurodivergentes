# Copie para ".env" neste diretorio. SECRET_KEY e JWT_ALGORITHM precisam
# ser IDENTICOS aos usados pela API principal (raiz do projeto, .env) -
# e o mesmo segredo que assina o access token que o usuario envia aqui.
SECRET_KEY=changeme-generate-a-strong-random-secret
JWT_ALGORITHM=HS256
