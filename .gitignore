# Ambiente
.env
.venv/
venv/
env/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
coverage.xml

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Docker
*.log

# Redis local (snapshot de persistencia gerado pelo redis-server)
dump.rdb

# Fotos de perfil enviadas por usuarios (app/foto_perfil) - arquivo local,
# nunca deve ir para o repositorio.
uploads/

# Alembic (mantemos as migrations, ignoramos apenas artefatos locais)
